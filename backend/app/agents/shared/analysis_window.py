"""분석 시청 윈도우 — 인덱서 ingest와 프로파일러 채점이 공유하는 단일 기간 손잡이.

이 값 하나만 바꾸면:
  - 인덱서: Takeout에서 catalog에 저장하는 시청 기록 범위
  - 프로파일러: 누적 catalog 중 21축 채점에 쓰는 최근 범위
가 함께 바뀐다. (Google Takeout 수집 주기를 정책적으로 바꿀 때 한 곳만 수정)
"""

WATCH_CATALOG_WINDOW_DAYS = 63  # 약 2개월(긴 달 2개=62일 + 여유 1일)
"""catalog 저장·프로파일러 채점 공통 시청 윈도우(일)."""
