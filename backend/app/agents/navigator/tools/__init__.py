"""Navigator LLM function-calling 도구 모음 (자리 예약 — 현재 비어 있음).

여기엔 **LLM이 직접 호출하는 도구**(types.Tool / function declaration)만 둔다.
규칙(코드가 부르는 계산)은 `axes.py`·`behavior_map.py`,
LLM 기능은 `ideal.py`·`graph.py`·`sub_agent/`에 있다.

(향후) LLM 도구가 필요해지면 이 자리에 추가한다.
"""
